var React = require('react');

var GraphSection = React.createClass({
    render: function() {
        return (
            <div id="GraphSection" />
        );
    }
});

module.exports = GraphSection;